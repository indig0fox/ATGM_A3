if (!isServer) exitWith {};

// The first option is used to indicate if each side should have their own unique language The second is is used to indicate if the radio channels for each team should be on different frequencies. So that players on other teams can not hear your communications.
// if (!isNil "acre_api_fnc_setupMission") then {[false, true] call acre_api_fnc_setupMission};